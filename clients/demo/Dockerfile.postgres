FROM postgres:15-alpine

COPY docker-entrypoint-initdb.d/ /docker-entrypoint-initdb.d/

# RubriCheck: Prisma on public. n8n: schema "n8n" (see 01-n8n-schema.sql).
# Existing DB volumes: run once: CREATE SCHEMA IF NOT EXISTS n8n;
