# Required
DATABASE_URL="postgresql://postgres:password@localhost:5432/rubricheck?schema=public"
JWT_SECRET=your-secret-at-least-32-chars-for-production

# Optional (local dev defaults)
PORT=8001
LOG_LEVEL=debug
APP_URL=http://localhost:5173

# Email (Resend) - for password reset and grading notifications
RESEND_API_KEY=
FROM_EMAIL=RubriCheck <onboarding@resend.dev>
N8N_PAPER_WEBHOOK_URL=
N8N_EXAM_PROJECTS_WEBHOOK_URL=
N8N_MOODLE_WEB_HOOK=
# Required for n8n callbacks - n8n must send this in header (default: X-Webhook-Secret)
N8N_WEBHOOK_SECRET=
N8N_WEBHOOK_HEADER=X-Webhook-Secret
MOODLE_WEB_SERVICE_URL=
WSTOKEN=
